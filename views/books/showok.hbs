<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/assets/css/bootstrap.css" rel="stylesheet" type="text/css" />
  <title>{{book.title}}</title>

</head>
<body>
  <h1 class="text-center">Libro {{book.title}}</h1>
  <div class="card container" style="width: 15rem;">
    <img src="/assets/uploads/{{book.image}}" class="card-img-top" alt="...">
    <div class="card-body">
      <h5 class="card-title">{{book.title}}</h5>
      <p class="card-text">{{book.author}}</p>
      <p class="card-text">{{book.description}}</p>
      <p class="card-text">{{book.category}}</p>
      <p class="card-text">$ {{book.price}}</p>
      <div class="text-center">
      <a href="/books/{{book._id}}/edit" class="btn btn-primary">Editar</a><p></p>
      <form action="/books/{{book._id}}/delete" method="post">
  <button class="btn btn-danger" type="submit">Eliminar</button>
  </div>
</form>
    </div>
  </div>
  

  <a href="/books">Volver al listado de productos</a>

     <script src="/assets/js/bootstrap.js"></script>
    <script src="/assets/js/bootstrap.bundle.js"></script>
</body>
</html>