<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/assets/css/bootstrap.css" rel="stylesheet" type="text/css" />
  <title>{{book.title}}</title>

</head>
<body>
  <h1 class="text-center">Libro {{book.title}}</h1>
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <img src="/assets/uploads/{{book.image}}" class="card-img-top" alt="..." style="max-width: 70%; height: auto;">
      </div>
      <div class="col-md-6">
        <div class="card" style="width: 100%;">
          <div class="card-body">
            <h5 class="card-title">{{book.title}}</h5>
            <h6>Autor</h6>
            <p class="card-text">{{book.author}}</p>
            <h6>Descripcion:</h6>
            <p class="card-text">{{book.description}}</p>
            <h6>Categoria:</h6>
            <p class="card-text">{{book.category}}</p>
            <h6>Precio:</h6>
            <p class="card-text">$ {{book.price}}</p>
            <div class="text-center">
              <a href="/books/{{book._id}}/edit" class="btn btn-primary">Editar</a>
              <p></p>
              <form action="/books/{{book._id}}/delete" method="post">
                <button class="btn btn-danger" type="submit">Eliminar</button>
              </form>
            </div>
 
          </div>
   
        </div>
                 <div class="p-3">
 <a href="/books" type="button" class="btn btn-success">Volver al listado de productos</a>
</div>
      </div>
    </div>
               
  </div>

</body>

  


     <script src="/assets/js/bootstrap.js"></script>
    <script src="/assets/js/bootstrap.bundle.js"></script>
</body>
</html>